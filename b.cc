#include "sorted_list.hpp"
#include <set>

using namespace leviathan::collections;


int main()
{
    std::set<int> ss;
    sorted_set<int> s;
    s.insert(1);
    std::cout << "Ok\n";
}
